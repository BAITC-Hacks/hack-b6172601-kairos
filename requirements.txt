# Ranges, not exact pins: an unverified pin that fails to resolve on the day is
# a disaster. Freeze exact versions during hour 1 with:
#   pip freeze > requirements.lock.txt
# and mention the lock file in the README.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
httpx>=0.27,<1.0
openai>=1.50,<3.0
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0

# Add when the case needs them:
# pandas>=2.2,<3.0          # tabular analysis
# python-multipart>=0.0.20  # file uploads
